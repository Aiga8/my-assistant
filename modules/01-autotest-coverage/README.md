# 🔍 Модуль сопоставления автотестов с TestRail

## Инструмент для сопоставления UI-автотестов с тест-кейсами в TestRail (Cursor + MCP)

## 📊 Функциональность

### 🎯 Назначение

Автоматизированное сопоставление существующих UI-автотестов (Playwright, TypeScript, репозиторий `solo-rtm`) с тест-кейсами в TestRail для анализа текущего покрытия и подготовки данных для дальнейшей разметки.

**Важно:**
Описание автотестов уже подготовлено и не генерируется в рамках этого модуля. Исходные файлы с описаниями не изменяются автоматически.

---

## ✅ Финальный флоу (актуальный процесс мэппинга)

Ниже описан повторяемый “боевой” процесс, который мы использовали, чтобы смэппить `modules/01-autotest-coverage/all-tests.md` (≈105 тестов) на TestRail **в рамках project_id=5**, без ограничений MCP по секциям/пагинации.

### Входные файлы

- **Источник описаний автотестов**: `modules/01-autotest-coverage/all-tests.md`
- **Человеко-редактируемая версия описаний (для тебя)**: `modules/01-autotest-coverage/all-tests-final.md`
  - Важно: текущий маппинг строился по `all-tests.md`/сабсетам, а `all-tests-final.md` используем как “спеку” и точечно синхронизируем названия/шаги.

### Выходные файлы (results)

Все артефакты сохраняем в:

- `results/01-autotest-coverage/`

Ключевые файлы:

- **Рабочий агрегированный файл** (один на всё): `results/01-autotest-coverage/all-tests-testrail-mapping.md`
- **Индивидуальные результаты маппинга по сабсетам**: `*-testrail-mapping-<dd_mm_yy_01>.{md,json}`
- **Экспорты TestRail по секциям (секция + все дочерние)**: `testrail-section-<sectionId>-subtree-cases.{md,json}`

---

## 🧰 Инструменты/скрипты (Node.js)

### 1) Экспорт TestRail кейсов по поддереву секции (обязательный шаг)

**Почему не MCP**: MCP `get_cases(section_id=...)` возвращает кейсы только самой секции; `get_sections` ограничен первой страницей секций. Поэтому используем прямой TestRail API с пагинацией.

Скрипт:

- `scripts/testrail_export_section_subtree.mjs`

Что делает:

- выгружает все секции проекта с пагинацией,
- строит поддерево от `root-section`,
- выгружает кейсы для каждой секции поддерева (с пагинацией 250),
- сохраняет `.md` и `.json` в `results/01-autotest-coverage/`.

Пример:

```bash
node scripts/testrail_export_section_subtree.mjs --project 5 --root-section <SECTION_ID> --out results/01-autotest-coverage
```

### 2) Фильтрация `all-tests.md` в сабсеты (чтобы маппить только релевантное)

Скрипты:

- `scripts/group_all_tests_by_path.mjs` — сгруппировать тесты по `path`
- `scripts/filter_all_tests_by_path.mjs` — вытащить тесты по точному `path`
- `scripts/filter_all_tests_by_keyword.mjs` — вытащить тесты по ключевым словам/путям (+ исключения, чтобы не тащить чужие группы)

Идея: пользователь даёт **section_id** под конкретный виджет/область, мы под него делаем сабсет тестов и маппим только его.

### 3) Маппинг сабсета автотестов на выгрузку TestRail

Основной скрипт маппинга:

- `scripts/geotarget_map_to_testrail.mjs`

Несмотря на имя, он универсальный (принимает любой `--input` и `--cases`).

Пример:

```bash
node scripts/geotarget_map_to_testrail.mjs ^
  --input results/01-autotest-coverage/all-tests-<subset>-only.md ^
  --cases results/01-autotest-coverage/testrail-section-<SECTION_ID>-subtree-cases.json ^
  --out-base results/01-autotest-coverage/<subset>-section<SECTION_ID>-testrail-mapping-<dd_mm_yy_01>
```

Скрипт сохраняет 2 файла:

- `<out-base>.md` (читаемый отчёт)
- `<out-base>.json` (источник истины для сборки рабочего файла)

### 4) Сборка единого “рабочего файла” без дублей

Скрипт:

- `scripts/rebuild_all_tests_working_file.mjs`

Что делает:

- берёт все `*-testrail-mapping-*.json` в `results/01-autotest-coverage/`
- собирает единый `all-tests-testrail-mapping.md`
- сортирует строго по исходному номеру теста из `all-tests.md` (sourceId)
- убирает дубли (одна запись на `sourceId`)
- позволяет “point fix”: новый маппинг для конкретного теста **перезаписывает** старый в рабочем файле

---

## 🧭 Как мы работаем итеративно (стратегия)

### Базовый цикл для нового виджета/области

1) Ты даёшь `section_id` в TestRail (где лежат кейсы по виджету/фиче).
2) Экспортируем кейсы поддерева:

```bash
node scripts/testrail_export_section_subtree.mjs --project 5 --root-section <SECTION_ID> --out results/01-autotest-coverage
```

3) Делаем сабсет тестов из `all-tests.md` под эту область (по `path` или по ключевым словам).
4) Запускаем маппинг сабсета на выгрузку кейсов.
5) (Опционально) Пересобираем рабочий файл:

```bash
node scripts/rebuild_all_tests_working_file.mjs
```

### Point fix (точечная правка одного теста)

Используем, когда:

- нашли более правильную секцию,
- нашли, что текущий TR-кейс “не тот”,
- нужно обновить заголовок/список кандидатов только для 1 теста.

Алгоритм:

1) Экспортируем нужную “узкую” секцию (например, `29094` для BHA Import):

```bash
node scripts/testrail_export_section_subtree.mjs --project 5 --root-section <SECTION_ID> --out results/01-autotest-coverage
```

2) (Опционально) делаем сабсет только с этим тестом.
3) Прогоняем маппинг и вносим точечную правку в `all-tests-testrail-mapping.md` (или пересобираем через rebuild, если правка приходит из JSON).

---

## 🧠 Правила маппинга (Covered / Partial / Not found)

### Скоринг и кандидаты

- **Exact/strong match**: один очевидный кейс → `Covered`
- **Похожие кейсы / покрытие “ядра”**: 2–3 кандидата или совпадает функциональное ядро → `Partial`
- **Нет релевантных кейсов в выгрузке** → `Not found`

### Важные исключения

- **Игнорируем визуальную регрессию/скриншоты** при расчёте релевантности (никакие `screenshot/snapshot/baseline/golden/...` не должны улучшать/ухудшать мэтч).

### Функциональное ядро (ключевой принцип для `Partial`)

- Перед сопоставлением выделяем “ядро”: минимальные функциональные действия+проверки, без которых сценарий теряет смысл.
- Если кейс покрывает ядро, но не покрывает дополнительные детали → всё равно включаем как минимум `Partial`.

---

## ✅ Договорённость по статусам Automation/Covered vs Partial (важно для разметки в TestRail)

Чтобы не путаться из‑за моков/стабов в UI‑автотестах, используем такое правило:

- **Automation/Covered** = **UI flow automated (mocked backend)**  
  То есть автотест считается “покрывающим” кейс, если он проверяет ключевой UI‑флоу и важные UI‑состояния/события, даже если бэкенд и импорт/парсинг данных замоканы.

- **Partial** = только когда в коде автотеста **нет важной части UI‑проверки**, например:
  - не проверяется, что **Drag & Drop реально работает** (нет видимого результата/нет подтверждения принятия файла),
  - не проверяется, что кнопка/действие **disabled для VIEWER**,
  - не проверяется, что **настройки/параметры реально отправились** (нет проверки запроса или эффекта сохранения),
  - или отсутствует другая ключевая проверка, без которой сценарий нельзя считать закрытым UI‑автотестом.

---

## 🧾 Как фиксировать связь в TestRail (практика разметки)

Рекомендуемая запись в поле `Refs` или комментарий (одинаковый формат везде):

`playwright/tests/<...>.spec.ts::test name`

Пример:

`playwright/tests/bha-import/bha-import.spec.ts::Complete BHA import functionality test`

Про `Partial`:

- В коде автотеста лучше ставить прямую ссылку на TR-кейс только если автотест реально “покрывает” кейс полностью.
- Если это `Partial`, лучше фиксировать это в TestRail (коммент/статус), а не в автотесте.

---

## 🧩 Промт-шаблон “на будущее” (копипаст)

Ниже шаблон для Cursor/LLM, когда нужно повторить процесс для нового виджета/секции.

### Контекст

- Project: `project_id=5`
- Входной файл описаний: `modules/01-autotest-coverage/all-tests.md` (или сабсет в `results/01-autotest-coverage/`)
- Рабочий файл результата: `results/01-autotest-coverage/all-tests-testrail-mapping.md`
- Поиск кейсов: **только** в `section_id=<SECTION_ID>` и всех дочерних (через экспорт скриптом)

### Инструкция

1) Экспортируй кейсы TestRail для `section_id=<SECTION_ID>` (включая дочерние) с помощью `scripts/testrail_export_section_subtree.mjs` и сохрани в `results/01-autotest-coverage/`.
2) Подготовь сабсет тестов из `all-tests.md`, который относится к этой секции (по `path` или ключевым словам). Сохрани сабсет в `results/01-autotest-coverage/all-tests-<subset>-only.md`.
3) Запусти маппинг сабсета на экспорт кейсов с помощью `scripts/geotarget_map_to_testrail.mjs` и сохрани `.md` и `.json` в `results/01-autotest-coverage/`.
4) Обнови `results/01-autotest-coverage/all-tests-testrail-mapping.md`, сохраняя:
   - сортировку по исходному номеру из `all-tests.md`,
   - отсутствие дублей (одна запись на один тест),
   - точечные фиксы должны “перезаписывать” старые результаты.

### Правила сопоставления (обязательно)

- Ищи кандидатов строго по выгруженным кейсам (не по MCP-поиску).
- При релевантности:
  - ≥60% ключевых слов → сильный кандидат
  - 30–60% → похожий кандидат
- **Игнорируй визуальную регрессию/скриншоты** (screenshot/snapshot/baseline/golden/visual/pixel/эталон/скриншот) при токенизации/скоринге.
- Используй принцип **функционального ядра**:
  - если ядро покрыто — минимум `Partial`
  - `Covered` ставь только при действительно сильном матче (или явном совпадении)

### Формат отчёта (md)

Для каждого теста:

- `### <sourceId>. <Название сценария>`
- Автотест: путь, название
- Найдено в TestRail: best match + alternate (или “полный список”, если автотест агрегирует много кейсов)
- Обоснование: коротко “почему”
- Статус: `Covered | Partial | Not found`
